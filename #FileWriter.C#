/**
 *
 *FileWriter.C
 *
 *
 *Authors: Adam Nieman & Victor Siu
 *
 *Comments:
 */

#include "FileWriter.h"

  String DEFAULT_NAME = "output.txt";


  FileWriter() {};

  public  void  OutputHTML( string fileName, vector<string> topWords, vector<string> misspelledWords )
  {
     FILE * file;

     //Open a writeable file with fileName.
     file = fopen(fileName.c_str(), "w");

     if(file = NULL)
       {
	 perror("Cannot write to file.");
       }
     else
       {
       }
  }

 public  void  OutputHTML( vector<string> topWords, vector<string> misspelledWords )
  {
    string fileName = DEFAULT_NAME;
  }
